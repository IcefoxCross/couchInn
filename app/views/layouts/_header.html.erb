<header>
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          
          <%= link_to root_path, class: 'navbar-brand' do %>
            <%= image_tag "logo.png", title: "Logo", class: "logo" %>
          <% end %>
          
<!--
          <button type="button" class="btn btn-default navbar-btn ">Sign in</button>
-->

        <div class="collapse navbar-collapse">
          
        </div><!--/.nav-collapse -->
      </div>

      <div class="btn-toolbar pull-right">

          <% if user_signed_in? && current_user.admin? %>
            <%= link_to("Tipos de Couch", types_path, class: "btn btn-success") %>
          <% end %>
          <% if user_signed_in? %>
             <%= link_to("Salir", logout_path, :method => :delete, class: "btn btn-default") %>
          <% else %>
             <%= link_to("Ingresar", login_path, class: "btn btn-default") %>
          <% end %>
      </div>

    </div>
  </header>
