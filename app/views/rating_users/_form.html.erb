<%= form_for(@rating_user) do |f| %>
  <% if @rating_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating_user.errors.count, "error") %> prohibited this rating_user from being saved:</h2>

      <ul>
      <% @rating_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
